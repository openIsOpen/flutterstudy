import 'package:flutter/material.dart';

class SwichAnimationFromRoutePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('TestRouteAnimationBar'),),
      body: Center(
        child: Text('TestRouteAnimation'),
      ),
    );
  }

}
